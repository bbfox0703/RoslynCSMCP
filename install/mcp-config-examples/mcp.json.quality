{
  "mcpServers": {
    "roslyn-quality": {
      "command": "dotnet",
      "args": ["run", "--project", "${workspaceFolder}/../RoslynCSMCP/src/RoslynMcpServer.Quality", "-c", "Release"],
      "env": {
        "DOTNET_ENVIRONMENT": "Production"
      }
    }
  }
}
